/target

# macOS metadata files
.DS_Store

# hypha spawn working directory
.cmn

# Python bytecode. The test smokes import each other, so running one writes a
# cache beside them — and a tracked .pyc is a binary the crate would publish.
__pycache__/
*.pyc
